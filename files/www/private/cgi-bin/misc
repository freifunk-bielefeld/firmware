#!/usr/bin/haserl
<%

case $GET_func in
  reboot)
    reboot
  ;;
  route)
    route
  ;;
  uname)
    uname -a
  ;;
  uptime)
    uptime
  ;;
  mac2ip)
    /sbin/mac2ip "$GET_mac"
  ;;
  *)
  echo "(E) misc: Invalid command: '$GET_func'"
  ;;
esac
%>
