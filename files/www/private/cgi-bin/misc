#!/usr/bin/haserl
<%

case $POST_func in
  reboot)
    reboot
  ;;
  route)
    route
  ;;
  uname)
    uname -a
  ;;
  uptime)
    uptime
  ;;
  *)
  echo "(E) misc: Invalid command: '$POST_func'"
  ;;
esac
%>
