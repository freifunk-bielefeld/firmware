#!/usr/bin/haserl
<%

case $GET_func in
  reboot)
    reboot
  ;;
  route)
    route
  ;;
  uname)
    uname -a
  ;;
  uptime)
    uptime
  ;;
  *)
  echo "(E) misc: Invalid command: '$GET_func'"
  ;;
esac
%>
