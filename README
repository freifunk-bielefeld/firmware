
Build Instructions:

mkdir OpenWrt/
cd OpenWrt/
svn co svn://svn.openwrt.org/openwrt/trunk/
cd trunk
#put files/ from git repository into the working directory
./scripts/feeds update -a
./scripts/feeds install -a
make defconfig
make menuconfig
# Select the following modules (or copy package/* from git into your working dir): 
# Target System => Atheros AR231x #for DIR-300
# Target System => Atheros AR71xx #for TL-WR841ND
# Target Profile => TP-LINK TL-WR841ND v7 #for TL-WR841ND
# Kernel modules => Network Support => kmod-batman-adv
# LuCI => Collections => luci #optional
# Network => Captive Portals => nodogsplash
# Network => VPN => n2n
# Network => Web Servers/Proxies => uhttpd
# Network => fping
# Utilities => haserl
make -j4

